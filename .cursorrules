# ⚡ Synapse (突触) - 核心产品架构与需求文档 (PRD)
## 1. 核心产品哲学 (Core Philosophy)
Synapse 不是一个传统的“题库”或“笔记本”，而是一个基于认知心理学和间隔重复 (SRS) 的“工程师第二大脑”。

万物皆卡片 (Everything is a Card)：最小的知识颗粒度是卡片（Node），而不是长篇大论的文章。

输入与输出解耦：学习时注重上下文连贯（走线），复习时注重随机抽取（打散）。

打破流畅性错觉：通过 UI 交互强迫用户进行“输出/检索”，拒绝“眼熟即掌握”。

## 2. 数据模型与架构 (Data Architecture)
### 2.1 容器：Topic (主题/题单)
知识的聚合体，拥有两种不同的渲染模式 (Mode)：

🧩 散装模式 (Discrete)：无序关联，适用于随机八股文（如“Go 语言常见面试题”）。

🗺️ 路线图模式 (Sequential/Path)：强前置关联，层层递进，适用于算法或系统学习（如“二叉树核心套路”）。包含解锁与闯关机制。

### 2.2 节点：Card (知识卡片)
底层统一为 Card 表，但分为三种形态 (Type)，拥有截然不同的 UI 交互：

📖 概念卡 (Concept)：单面阅读。

交互：支持 Markdown。核心词使用 == 语法生成**“刮刮乐遮罩 (Cloze Deletion)”**。

检验：底部点击掌握时，弹出全局**“费曼三问拦截框”**。

🗣️ 问答卡 (Q&A)：双面盲盒。

交互：正面问题，背面答案。必须点击“翻转”才能查看，随后进行记忆分级评估。

💻 算法卡 (Algorithm)：实战演练。

交互：包含题目、输入输出示例。带有明确的难度评级 (Easy/Medium/Hard)。

## 3. 技术栈规范 (Tech Stack)
后端: Go (Gin) + GORM + MySQL。强调高并发与极简接口。

前端: React 18 + TypeScript + Tailwind CSS + Framer Motion。

设计语言: Glassmorphism (玻璃拟态)、极简主义、Dark Mode 友好。强调微交互（Hover 态、平滑过渡、瀑布流入场）。

## 4. 当前开发阶段重点 (Current Focus)
前端先行 (UI-First)：优先构建界面骨架与交互逻辑，使用 Mock 数据验证产品链路。

当前任务：实现 TopicPath.tsx (题单闯关路线视图)，验证路线图模式下的时间轴 UI 与多类型卡片的混合渲染。

## 5. 开发进度追踪 (Progress Tracker)
[说明：AI 助手在每次接到新指令时，必须优先阅读此部分，以明确当前系统的实现状态，避免重复开发或覆盖已有代码。]

### 🟢 已完成 (Done)
- [x] 确立了 Synapse 的核心产品哲学与“万物皆卡片”架构。
- [x] 设计了概念卡(Concept)的交互模型。
- [x] 前端实现 `TopicPath.tsx` (路线图模式下的时间轴 UI)。
- [x] 开发 `CardDetailDrawer` 右侧抽屉。
- [x] 重构 `AllCardsView.tsx` (全局散装卡片列表)。
- [x] 重构 `Sidebar.tsx`，实现父子层级的树状导航。
- [x] 开发后台管理台 `AdminStudio.tsx`，实现左侧架构树与右侧多态卡片编辑器的双栏沉浸式录入体验。
- [x] 开发后台管理台 `AdminStudio.tsx` (创造者工作室)，包含上下文感知的树状导航与带实时预览的 Markdown 编辑器
- [x] 深化 `AdminStudio.tsx` (创造者工作室) 的左侧结构树，支持“散装卡片”与“路线图”的混合挂载
- [x] 重构 Go 后端 GORM 数据模型 (Models)



### 🚧 当前冲刺 (Current Sprint / In Progress)
- [ ] 基于 Gin 和 GORM 开发创造者工作室 (Creator Studio) 所需的 RESTful API

### ⚪ 待开发池 (Backlog / To Do)
- [ ] 后端：Go (Gin) + GORM 实现 Topic 与 Card 的数据模型及 CRUD 接口。
- [ ] 前端：实现卡片正文的 Markdown 解析与 `==关键词==` 遮罩组件。
- [ ] 前端：实现全局的“费曼三问”确认弹窗 Modal。
- [ ] 后端：设计并实现基于间隔重复 (SRS) 的复习调度算法。
- [ ] 全栈：实现极速录入卡片的 Markdown 编辑器与解析逻辑。